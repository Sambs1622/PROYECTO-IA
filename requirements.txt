# Asistente de Voz TPM - Dependencias
# Instalar con: pip install -r requirements.txt

# LiveKit Agents framework + plugins
livekit-agents>=0.11.0
livekit-plugins-deepgram>=0.6.0
livekit-plugins-google>=0.3.0
livekit-plugins-openai>=0.10.0
livekit-plugins-silero>=0.7.0

# LiveKit SDK para token generation
livekit>=0.17.0

# API Server
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# Data processing
pandas>=2.2.0
openpyxl>=3.1.0

# Utilities
python-dotenv>=1.0.0
aiohttp>=3.9.0
